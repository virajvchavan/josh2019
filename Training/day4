default parameters
hash as a parameter
Ranges
.. and ...
nums.each{|num| puts num}
nums.collect
nums.collect!
nums.select
nums.select!
nums.map
nums.map!
h1.merge(h2)  -stores result in new hash 
h1.merge!(h2) -stores result in h1
nested hash
blocks
yield
  def select
    name
    yield(name)
  end
  select{ |a| puts 'hello' + a }

def select(&viraj)
sort an array of hashes
